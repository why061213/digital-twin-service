# 粤E55170 固定案例（2026-07-27）

## 锁定目的

把 2026-07-27 地图中粤E55170对应的数据固定为可重复回放的测试案例，避免外部订单、车辆位置和运行缓存变化后无法复现。

固定夹具：`src/test/resources/fixtures/vehicle-order-chain/yue-e-55170-2026-07-27.json`

## 系统当时实际做了什么

1. 车辆历史中出现过两个订单：
   - `CY0220260722068`：2026-07-22，南拓港仓 → 清远石角。
   - `CY0220260727012`：2026-07-27，南拓港仓 → 大沥。
2. 当前车辆订单链只保留 `CY0220260727012`；7月22日订单没有进入当前 trip。
3. 当前订单被确认为已提货并在途：
   - `decision = EN_ROUTE_TO_DELIVERY`
   - `tripPhase = LINEHAUL`
   - `targetAction = DELIVERY`
   - `groupEligible = true`
4. 当前 trip 只有一个 `orderInstanceId`：
   `CY0220260727012|CY0220260727012-1|粤E55170`。
5. 地图上同车牌的两枚文字标签是当前订单的起点和终点，不足以证明两个订单被合并。

## 回归边界

`YueE55170SnapshotReplayTest` 固定验证：

- 历史订单与当前 trip 必须区分。
- 当前订单进入 `CONFIRMED + onboard + LINEHAUL`。
- 当前目标是大沥的 `DELIVERY`。
- 历史订单不得因相同车辆而自动并入当前 trip。

后续测试真正的动态多单合并时，应在此夹具基础上显式加入第二个仍有效的当前订单，并分别观察 `QUEUED`、动态插单成功和动态插单拒绝三种结果。
