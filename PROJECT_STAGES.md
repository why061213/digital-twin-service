# 项目阶段推进

## 阶段 1：创建后端项目

目标：

- 创建 Spring Boot Maven 项目
- 能在 IntelliJ IDEA 2023 中打开
- 能成功启动服务

验收：

- 浏览器访问 `http://localhost:8080/api/health`
- 返回 `status: ok`

当前状态：已完成。

## 阶段 2：WebSocket 主动推送

目标：

- 提供 WebSocket 地址
- 前端连接后，后端主动推送消息
- 使用固定 token 做简单校验

验收：

- 正确 token 可以连接
- 错误 token 连接失败
- 前端无需轮询即可收到消息

当前状态：已完成。

## 阶段 3：实时模拟数据

目标：

- 每 3 秒推送 KPI、库存、车辆、能耗
- 消息格式统一为 `type + timestamp + data`

验收：

- 前端控制台能连续收到 `kpi`、`inventory`、`vehicle`、`energy`
- 每类数据都能独立分发给对应大屏模块

当前状态：已完成。

## 阶段 4：业务线路和飞线

目标：

- 每 5 到 10 秒随机生成一条城市对线路
- 推送 `city_raise`
- 5 到 10 秒后自动推送 `city_fall`
- 支持多条线路并发存在

验收：

- `city_raise` 中包含起点城市、终点城市、经纬度、线路值、持续时间
- `city_fall` 中包含同一个 `routeId`
- 前端可以按 `routeId` 移除对应飞线

当前状态：已完成。

## 阶段 5：对接前端大屏

目标：

- 把前端原来的静态数据改成 WebSocket 数据驱动
- 大屏打开时自动带 token 连接
- 根据 `message.type` 更新对应模块

建议顺序：

1. 先只在前端控制台打印 WebSocket 消息。
2. 再把 KPI、库存、车辆、能耗接到已有面板。
3. 最后接入 `city_raise` 和 `city_fall` 控制城市升起和飞线。

当前状态：待开始。

## 阶段 6：从模拟数据切换到真实业务数据

目标：

- 将当前随机数据生成器替换成数据库、第三方接口或设备数据
- 保留 WebSocket 推送协议不变

验收：

- 前端不需要大改
- 后端只替换数据来源

当前状态：待开始。
